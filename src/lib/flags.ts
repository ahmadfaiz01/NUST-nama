/**
 * Features that exist in the codebase but are not open to students yet.
 *
 * Gupshup's threads work, but there is no moderation on them — no reporting, no
 * flagging, no way to take a post down quickly. Opening a forum on a campus
 * without that is how you end up moderating by hand at 3am. The pages and tables
 * stay; the doors are shut until moderation exists.
 */
export const GUPSHUP_ENABLED = false;
